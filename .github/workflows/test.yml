name: CI
on:
  workflow_dispatch:

jobs:
  build:
    permissions:
      id-token: 'write' 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a multi-line script
        run: |
          pip install sigstore
          python3 test.py
